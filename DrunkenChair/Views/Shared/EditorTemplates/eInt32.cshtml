@model Int32

@{
    var fieldName = ViewData.TemplateInfo.HtmlFieldPrefix;

    var min = ViewBag.min;
    var max = ViewBag.max;
    //bool disableDirectEdit = ViewBag.disableDirectEdit;
    //var dde = "";
    //if(disableDirectEdit)
    //{
    //    dde = Html32TextWriter("onkeydown = return false");
    //}
}

<input class="text-box single-line valid" data-val="true" data-val-number="The field @fieldName must be a number."
       data-val-required="The @fieldName field is required." id=@fieldName name=@fieldName type="number" value="0" max=@max min=@min>